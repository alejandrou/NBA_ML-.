# Historical feature specifications

These files are the historical contracts for completed work. They are retained
in place for human reference and Git history, but are not Codex startup context.
Do not create new feature specifications here: active and future work uses
`tasks/` cards, and phase scope belongs in `docs/roadmap/`.
