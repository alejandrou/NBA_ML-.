---
task_id: WF-001
title: Establish the canonical repository workflow
phase: maintenance
task_type: docs
status: done
mode: implement
skills: [docs-maintenance]
must_read: [AGENTS.md]
allowed_paths: [AGENTS.md, tasks, docs/roadmap, README.md]
forbidden_paths: [src/nba_data, tests, alembic, scrap, models, db_manager, utils]
validation: [python scripts/harness/validate_workflow.py, git diff --check]
requires_owner_approval: false
owner_approved: false
approval_scope:
next_task: tasks/maintenance/WF-002.md
---

# Goal

Replace competing workflow authorities with a pointer, executable task cards,
and a single roadmap without implementing Phase 5 API work.

# Scope

Create the canonical pointer, card template, migration cards, roadmap, and
stable repository instructions. Preserve F5-001 as a future API task.

# Acceptance criteria

- `tasks/CURRENT.md` is the sole active-task pointer.
- `ROADMAP.md` owns phase state.
- Phase 4E is recorded done and Phase 5 remains planned pending owner approval.

# Constraints

Do not implement API code or change production, database, scraping, or tests.

# Review and handoff

Validation: workflow validation and `git diff --check` passed.

Review: canonical pointer, template, roadmap, and stable authorities established.

Remaining risks: none for this task.

Next task: WF-002
