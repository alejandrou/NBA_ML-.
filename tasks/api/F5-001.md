---
task_id: F5-001
title: Implement the read-only FastAPI foundation
phase: phase-5-api
task_type: api
status: review
mode: implement
skills: [api-fastapi, testing]
must_read:
  - AGENTS.md
  - docs/architecture/API_ARCHITECTURE.md
  - docs/architecture/API_CONTRACT.md
  - .agents/skills/api-fastapi/references/architecture.md
  - .agents/skills/api-fastapi/references/testing.md
allowed_paths: [pyproject.toml, uv.lock, src/nba_data/api, tests, docs/architecture, tasks/api/F5-001.md, tasks/CURRENT.md]
forbidden_paths: [src/nba_data/scraping, src/nba_data/db/models, src/nba_data/db/repositories, src/nba_data/db/session.py, scrap, models, db_manager, alembic]
validation: [uv run ruff check ., uv run pytest, git diff --check]
requires_owner_approval: false
owner_approved: false
approval_scope:
next_task: tasks/api/F5-002.md
---

# Goal

Implement the offline, read-only FastAPI foundation: compatible FastAPI/Uvicorn
dependencies if required, `src/nba_data/api`, `create_app()`, `/api/v1`, and
`GET /api/v1/health` with `HealthResponse`.

# Scope

Create only the approved API foundation and offline tests for its exact schema
and OpenAPI registration.

# Acceptance criteria

- The app factory and health endpoint return 200 with the approved schema.
- OpenAPI contains health and no mutation or unapproved routes.
- Health has no database or network access; all tests are offline.

# Constraints

Do not introduce database access, network calls, mutation routes, scraping,
migrations, authentication, or other resources.

# Review and handoff

Validation: `uv run pytest tests/unit/test_api_foundation.py` passed with 5 tests. Declared validation passed: `uv run ruff check .`, `uv run pytest` (324 passed, 1 integration deselected), and `git diff --check`.

Review: Implemented `create_app()`, versioned `/api/v1`, `GET /api/v1/health`, explicit `HealthResponse`, and offline contract/OpenAPI tests without DB, network, auth, scraping, or mutation routes.

Remaining risks: FastAPI emits a `StarletteDeprecationWarning` from `fastapi.testclient` about future `httpx2` usage; no functional failure.

Next task: F5-002
