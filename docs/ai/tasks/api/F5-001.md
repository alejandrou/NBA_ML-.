---
task_id: F5-001
title: Design the read-only FastAPI foundation
phase: phase-5-api
task_type: api
status: planned
mode: plan
skills: [api-fastapi, db-readonly, testing]
must_read: [AGENTS.md, docs/ai/CURRENT_TASK.md, docs/architecture/API_ARCHITECTURE.md, docs/architecture/API_CONTRACT.md]
allowed_paths: [src/nba_data/api, tests, docs/architecture]
forbidden_paths: [scrap, models, db_manager, alembic]
validation: [uv run ruff check ., uv run pytest]
next_task: docs/ai/tasks/api/F5-002.md
---

# Scope

Define, then implement only after approval, the FastAPI app factory at
`src/nba_data/api/app.py`, the `/api/v1` prefix, and `GET /api/v1/health`.

## Constraints

Use TestClient and explicit response models. The API is read-only, does not
scrape, does not write to the database, has no authentication initially, and
does not include complex business endpoints.
