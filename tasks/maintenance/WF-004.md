---
task_id: WF-004
title: Remove the legacy workflow and validate task cards
phase: maintenance
task_type: docs
status: done
mode: implement
skills: [docs-maintenance, testing]
must_read: [AGENTS.md, tasks/TEMPLATE.md]
allowed_paths: [docs/ai, progress, tasks, scripts/harness, docs/roadmap, docs/decisions, docs/validation]
forbidden_paths: [src/nba_data, tests, alembic, scrap, models, db_manager, utils]
validation: [python scripts/harness/validate_workflow.py, uv run ruff check ., uv run pytest, git diff --check]
requires_owner_approval: false
owner_approved: false
approval_scope:
next_task: tasks/maintenance/WF-005.md
---

# Goal

Replace legacy harness dependencies and remove obsolete workflow sources.

# Acceptance criteria

- The validator checks pointer, cards, skills, paths, approvals, and phase rules.
- No active reference remains to the retired workflow directories or manual board.
- Harness validation runs the workflow validator before lint and tests.

# Constraints

Do not remove durable architecture, validation, phase, or ADR documents.

# Review and handoff

Validation: workflow validator, Ruff, Pytest, and `git diff --check` passed.

Review: legacy workflow sources and harness dependencies were removed; validator is active.

Remaining risks: Phase 5 handoff awaits owner decision.

Next task: WF-005
