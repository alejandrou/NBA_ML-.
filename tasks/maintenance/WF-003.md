---
task_id: WF-003
title: Consolidate repository skills
phase: maintenance
task_type: docs
status: done
mode: implement
skills: [docs-maintenance]
must_read: [AGENTS.md, tasks/TEMPLATE.md]
allowed_paths: [.agents/skills, tasks]
forbidden_paths: [src/nba_data, tests, alembic, scrap, models, db_manager, utils]
validation: [python scripts/harness/validate_workflow.py, git diff --check]
requires_owner_approval: false
owner_approved: false
approval_scope:
next_task: tasks/maintenance/WF-004.md
---

# Goal

Retain only current, non-overlapping workflow skills.

# Acceptance criteria

- `db-schema` and `review` replace their overlapping predecessors.
- Cards reference only existing skills.
- Speculative frontend and feature skills are absent.

# Constraints

Do not change runtime implementation.

# Review and handoff

Validation: workflow validation and `git diff --check` passed.

Review: `db-schema` and `review` replace overlapping and speculative skills.

Remaining risks: none for this task.

Next task: WF-004
