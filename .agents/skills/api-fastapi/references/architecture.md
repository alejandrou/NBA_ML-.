# API reference: architecture

Keep HTTP translation in routers, use-case composition in services, and
read-only persistence in repositories. The app factory owns versioned router
registration and dependency wiring.
