# API reference: testing

Use FastAPI TestClient. Assert status codes, response schemas/shapes, empty
collections, invalid filters, and missing resources. Tests must use local
fixtures or dependency overrides and must never scrape.
